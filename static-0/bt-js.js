document.getElementById('download').innerHTML = "Download BT-JS " + btjsVersion;
document.getElementById('change-log').innerHTML = btjsVersion + " Change Log";

function link(l) {
	window.location.assign(l)
}